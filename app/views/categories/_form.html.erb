<%= form_for(@category) do |f| %>
  <%= flash_and_form_errors(@category)%>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
