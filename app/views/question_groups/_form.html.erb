<%= form_for(@question_group) do |f| %>
  <% if @question_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_group.errors.count, "error") %> prohibited this question_group from being saved:</h2>

      <ul>
      <% @question_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :required => "true" %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :question_groups_type %><br />
    <%= select("question_group", :question_groups_type,{ "1" => "Survey", "2" => "Test", "3" => "Exam" }) %>
  </div>
  <div class="field">
    <%= f.label "Add questions to your " %><br />
    <%= f.number_field :question_groups_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
